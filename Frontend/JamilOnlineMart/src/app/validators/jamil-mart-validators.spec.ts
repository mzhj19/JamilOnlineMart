import { JamilMartValidators } from './jamil-mart-validators';

describe('JamilMartValidators', () => {
  it('should create an instance', () => {
    expect(new JamilMartValidators()).toBeTruthy();
  });
});
